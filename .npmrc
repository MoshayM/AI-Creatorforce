approve-builds=@sentry/cli,@sentry/profiling-node
# Hoist all packages to root node_modules/ so Vercel's node-file-trace can
# resolve them without following pnpm's .pnpm symlink chain.
shamefully-hoist=true
